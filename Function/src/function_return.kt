fun main() {
    println(add(5, 5)) // Calling the add function and printing the result
}

// In functions, we usually return some value.
// Here the function is returning the sum of two numbers.
fun add(num1: Int, num2: Int): Int {
    return num1 + num2 // Returning the sum of num1 and num2
}
